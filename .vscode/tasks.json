{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: build active exercise",
            "type": "shell",
            "command": "[ -f PG2026-2/build/CMakeCache.txt ] || cmake -S PG2026-2 -B PG2026-2/build; cmake --build PG2026-2/build --target ${fileBasenameNoExtension}",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compila o exercicio do arquivo aberto (o alvo CMake tem o mesmo nome do .cpp)"
        }
    ]
}
